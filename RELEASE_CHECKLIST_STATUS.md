# Paracord Release Checklist Status

Date: 2026-05-18

Status key:
- DONE: implemented and supported by local evidence.
- PARTIAL: implementation or automated evidence exists, but release-grade validation is incomplete.
- BLOCKED: cannot be completed in this local environment.
- OWNER: requires a release-owner decision.
- TODO: not yet verified or not yet completed.

Structural validation: `python scripts\validate_release_checklist_status.py` passes, covering 13 sections, 149 source goal bullets, and 155 status entries: 96 DONE, 49 PARTIAL, 8 BLOCKED, 2 OWNER, 0 TODO.

This file maps each explicit item from `goal.txt` to the current evidence or remaining gap. It is intentionally conservative: automated tests are treated as evidence only for the behavior they actually cover.

## 1. Stabilize The Release Scope

- PARTIAL: Decide what belongs in this release versus later. Evidence: `RELEASE_WORKTREE_CLASSIFICATION.md` classifies source/docs/generated/local files. Gap: owner still needs to approve the final public scope.
- PARTIAL: Remove accidental/generated/unwanted files from the working tree. Evidence: `.gitignore` now covers several generated/local outputs. Gap: working tree is still very large and must be staged intentionally.
- PARTIAL: Review every untracked source/doc/migration/package file and classify it. Evidence: `RELEASE_WORKTREE_CLASSIFICATION.md` was refreshed on 2026-05-18, and a local `git status --short --untracked-files=all` coverage check confirms every current untracked path is explicitly listed or matched by a documented release/internal/generated classification pattern. Gap: owner decisions remain for internal reports, SDK license, and vendored `third_party/scap` strategy.
- PARTIAL: Split the giant pending update into logical commits. Evidence: `RELEASE_WORKTREE_CLASSIFICATION.md` classifies the worktree and `docs/release-commit-plan.md` defines a staged commit sequence. Gap: commits have not been made and owner decisions are still required before staging.
- DONE: Update versioning consistently across root Cargo, client package, Tauri config, README/release notes. Evidence: root `Cargo.toml`, `client/package.json`, `client/src-tauri/tauri.conf.json`, `README.md`, and `RELEASE_NOTES.md` all reference `0.9.0`; `python scripts\check_release_version.py` and `python scripts\check_release_version.py v0.9.0` both pass.

## 2. Make CI Green Locally

- DONE: `cargo fmt --all -- --check`. Evidence: rerun on the latest tree on 2026-05-18 after client/profile feedback hardening.
- DONE: `cargo clippy --workspace -- -D warnings`. Evidence: rerun on the latest tree on 2026-05-18 after client/profile feedback hardening.
- DONE: `cargo check --workspace --no-default-features`. Evidence: rerun on the latest tree on 2026-05-18 after client/profile feedback hardening.
- DONE: `cargo test --workspace`. Evidence: rerun on the latest tree on 2026-05-18 after client/profile feedback hardening.
- DONE: `cargo test --workspace --all-targets`. Evidence: rerun on the latest tree on 2026-05-18 after client/profile feedback hardening.
- DONE: `cd client && npm run typecheck`. Evidence: rerun on the latest tree after admin backup/security UI smoke expansion.
- DONE: `cd client && npm run test:unit`. Evidence: latest rerun after UserPanel identity-button accessibility and username-copy failure coverage, UserProfile DM/friend/block/report API error-detail coverage, shared Button default-type coverage, FileUpload accessible upload/remove control coverage plus unsafe-image-MIME fallback, CommandPalette modal/focus coverage, CommandBuilder command create/update error-detail and option-control accessibility coverage, CreateGuildModal create/join/template coverage, RegisterPage password-confirmation/trim/server-list coverage, InviteModal generate/copy failure coverage, InvitePage preview/login/accept coverage, DiscoveryPage concrete load-error/join/no-public-invite/join-failure coverage, HomePage dashboard DM success/failure coverage, FriendsPage action feedback/pending-guard coverage, DM/group-DM error-state coverage, GuildChannelList inline channel-create accessibility/success/failure, copy-ID failure feedback, and leave-server failure-feedback coverage, ChannelManager channel admin action and announcement-follow error-detail coverage, ChannelPermissionsEditor overwrite load/save/add error-detail coverage, Sidebar server copy-ID failure feedback, MemberList member copy-ID failure feedback, TopBar pinned-message load/unpin failure feedback plus unsafe/external-avatar fallback coverage, TopBar inbox load-failure feedback coverage, TopBar summary failure alert coverage, TopBar announcement-channel follow/unfollow failure feedback coverage, SearchPanel and global SearchOverlay control/error accessibility coverage, TopBar DM voice-call failure feedback coverage, MessageComponents button/select/entity-load failure feedback and unsafe-link blocking coverage, safe entity-avatar rendering/fallback, MessageEmbed/GitHubEventEmbed unsafe-link and unsafe image coverage, FilePreview unsafe attachment URL and unsafe-image-MIME fallback coverage, ImageLightbox unsafe resource URL blocking, ErrorBoundary bug-report URL fallback, ForumView search/create-post error-detail feedback coverage, scheduled-event calendar URL path-segment encoding plus event load/action error-detail coverage, MessageList reaction/pin failure-feedback plus unsafe sticker/attachment URL fallback coverage, ThreadPanel restore/delete error-detail coverage, LoginPage password-reset/MFA flow coverage, account setup/recovery phrase UX/name-trim coverage, account unlock redirect/recovery/import/cooldown/reconnect coverage, UserSettings profile/settings/MFA error-detail coverage, onboarding gate/settings UI coverage, anonymous/disappearing-message display coverage, slowmode send-error feedback coverage, composer attachment upload coverage plus unsafe-image-MIME fallback, scheduled-message composer schedule-mode/failure/future-time validation coverage, sticker/GIF quick-send API error-detail coverage, gateway RESUMED queue flush/reconnect bookkeeping coverage, one-to-one DM E2EE send/locked-key store coverage, sticker/custom-emoji picker/composer-send plus retryable sticker-load error details, guild emoji-management UI coverage, unsafe GIF/sticker resource fallback coverage, custom emoji token-aware image URL coverage, OAuth redirect userinfo blocking, screen-share thumbnail URL safety, economy leaderboard/settings UI coverage, moderation-template create/apply/delete UI coverage, template-gallery browse/create/delete/error coverage, bot review/authorization load and authorization error-detail coverage, public store-card and BotStoreSection load/install/add error-detail coverage, developer metrics/create-form accessibility, token-copy failure-detail coverage, developer create-bot API error-detail coverage, guild webhook error-detail coverage, desktop/API log redaction coverage, admin security-event pagination/details UI coverage, server-root URL coverage for federation admin routes, expanded custom-CSS/external/resource-URL sanitizer coverage, stored image data URL safety for guild icons/banners, profile banners, bot-store icons, entity avatars, and Markdown autolink external-URL gating; 91 files / 513 tests passed.
- DONE: `cd client && npm run test:e2e`. Evidence: rerun on the latest tree after client/profile feedback hardening and rebuilt production client; Playwright smoke passed.
- DONE: `cd client && npm run build`. Evidence: rerun on the latest tree after client/profile feedback hardening; entry JS is `assets/index-BXkAOfWY.js` at 341.64 kB and largest JS chunk remains `assets/vendor-livekit-DQ-t7ERF.js` at 437.47 kB.
- DONE: `cd client && npm run test:contrast`. Evidence: rerun on the latest tree after client/profile feedback hardening; configured themes pass WCAG contrast checks.
- DONE: `python scripts/ci_migration_sanity.py`. Evidence: latest rerun after client/profile feedback hardening applies 65 SQLite migrations and checks 66 PostgreSQL migrations for filename parity plus obvious dialect drift.
- DONE: `python scripts/security_gate_check.py`. Evidence: latest rerun after client/profile feedback hardening reads the public `docs/security-release-gate.md` gate file and reports all 10 P0 tasks are DONE.
- DONE: GitHub Actions workflow lint. Evidence: digest-pinned `docker run --rm -v "${PWD}:/repo" -w /repo rhysd/actionlint@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667 .github/workflows/ci.yml .github/workflows/release.yml .github/workflows/security-audit.yml .github/workflows/security-dast-fuzz.yml` passes after fixing release-artifact glob verification to use `compgen -G` and a `security-dast-fuzz.yml` shellcheck warning for an unused health-wait loop variable; CI's migration/feature sanity job and the tag-triggered release validation job now run the same digest-pinned actionlint command plus release version, dependency-audit, checklist-status, line-ending, Python-syntax, and shell-syntax guards.
- DONE: `git diff --check`. Evidence: exits 0 after fixing `ErrorBoundary.tsx`; the release-critical scoped diff check for workflows, migrations, release scripts, and release docs is warning-free, while the full dirty worktree can still print non-failing `core.autocrlf` conversion warnings for non-pinned source/doc files.

## 3. Fix Current Known Build/Test Breakages

- DONE: Fix `paracord-db` test compile error in `attachments.rs` by using typed `GuildId`/`UserId`. Evidence: full Rust test suites pass.
- DONE: Fix `MessageInput.test.tsx` mock drift by adding `channelApi.getFeatureSettings`. Evidence: unit tests pass.
- DONE: Fix rustfmt diffs in native media, relay, room, and transport. Evidence: `cargo fmt --all -- --check` passes.
- DONE: Fix Clippy failures in `paracord-codec`, `paracord-transport`, and downstream crates. Evidence: Clippy passes with `-D warnings`.
- DONE: Fix no-default-feature VPX gating problems in Tauri native media. Evidence: `cargo check --workspace --no-default-features` passes.
- DONE: Decide whether no-default-features is supported. Evidence: CI/local gate keeps `--no-default-features`, while docs state `vpx` is required for release media behavior and should not be disabled to work around build issues.

## 4. Security Fixes Before Release

- DONE: Add max handshake payload size before allocation in QUIC media connection handshake. Evidence: `MAX_AUTH_MESSAGE_SIZE` bounds pre-auth allocation in `crates/paracord-transport/src/connection.rs`.
- DONE: Recheck active auth session/current voice state during WebTransport accept. Evidence: WebTransport path resolves active media room from DB voice state and session id in `crates/paracord-server/src/main.rs`.
- DONE: Stop trusting media token room claim as authority. Evidence: `resolve_active_media_room` cross-checks DB voice state/session and rejects mismatched claimed room.
- DONE: Shorten media token lifetime or bind it to revocable session/voice-state state. Evidence: token now carries `sid` and accept paths require matching active voice state.
- DONE: Add tests for expired/revoked/left-voice WebTransport media tokens. Evidence: `cargo test -p paracord-transport expired_media_claims_are_rejected -- --quiet` verifies expired media JWTs fail validation with `ExpiredSignature`; `cargo test -p paracord-server resolve_active_media_room -- --quiet` verifies the media accept guard requires the current DB voice session, rejects claimed-room mismatches, rejects stale sessions after leave, and keeps DM voice scoped to `0:{channel_id}`; and `cargo test -p paracord-api --test voice_routes -- --quiet` verifies stale leave requests do not clear newer voice sessions, native DM voice issues `0:{channel_id}` media-token room claims, creates a revocable DB voice state, removes that state on active-channel leave, forbids non-member DM voice leave, and preserves an active DM voice state when a different DM is left.
- DONE: Add SSRF/private-IP validation to admin federation server discovery. Evidence: federation URL validation rejects private/reserved addresses and metadata hosts; OpenGraph preview fetching now uses the same broader reserved/documentation/multicast/local-domain blocking for user-controlled link previews.
- DONE: Keep desktop self-signed TLS bypass scoped to trusted servers. Evidence: WebView2 certificate overrides plus Tauri `probe_server` and `native_fetch` now accept self-signed certificates only for loopback or exact trusted server origins; the renderer-facing trusted-server sync command verifies each non-loopback candidate's `/health` endpoint identifies as Paracord before storing that origin, so renderer input cannot directly whitelist arbitrary TLS targets; `cargo test -p paracord-desktop --lib -- --quiet` covers trusted loopback/configured origins, different-port rejection, untrusted external, relative/non-HTTP rejection, and trusted-origin/health-URL parsing.
- DONE: Enforce or remove `federation.allow_discovery`. Evidence: federation defaults to disabled and discovery route checks `service.allow_discovery()`.
- DONE: Fix federated file cache authorization. Evidence: targeted federation/file security regressions pass.
- DONE: Replace homegrown federation file token MAC with HMAC and constant-time comparison. Evidence: federation route uses `hmac` and verification path.
- DONE: Add pre-auth WebSocket per-IP/connection caps or reduce identify timeout. Evidence: gateway has configurable global and per-user connection caps plus user-level message/presence/typing/voice rate limiters; latest `python scripts/release_ws_pre_auth_capacity_smoke.py --port 18127 --max-connections 3 --connections 5` starts the release binary with a tiny pre-auth global cap and verifies exactly 3 unauthenticated sockets receive HELLO while 2 overflow sockets are closed.
- DONE: Run npm audit fix or manually upgrade vulnerable packages. Evidence: latest `npm audit --audit-level=moderate` passes for client and bot SDK with 0 vulnerabilities.
- DONE: Avoid surprising AWS credential discovery for optional S3-compatible storage. Evidence: `S3Config` now defaults `use_aws_credential_chain` to false, requires explicit `access_key_id`/`secret_access_key` unless that opt-in is set, README/config/self-hosting docs state local filesystem storage is the default and S3-compatible object storage is opt-in, `cargo test -p paracord-server config::tests -- --quiet` verifies default local storage and that S3 env vars do not select S3 without an explicit storage-type override, and latest `cargo check -p paracord-media --features s3`, `cargo check -p paracord-server --features s3`, `cargo test -p paracord-media --features s3 -- s3_ --quiet`, and `cargo clippy -p paracord-server --features s3 -- -D warnings` pass.
- PARTIAL: Run Rust dependency audit and fix/waive intentionally. Evidence: latest `cargo audit` exits 0 with no unignored vulnerability failures, and `docs/release-risk-waivers.md` documents the explicit ignore plus 23 current warning waivers. `cargo audit -D warnings` fails on those warnings by design until the release owner accepts or resolves them. Gap: release owner must still accept or reject those waivers before publishing.

## 5. Database And Migration Readiness

- DONE: Add missing PostgreSQL migration for `messages.embeds`. Evidence: matching SQLite/PostgreSQL `20260228000001_message_embeds.sql` files exist and migration sanity passes.
- DONE: Verify SQLite and PostgreSQL migration parity for every new feature table/column. Evidence: `scripts/ci_migration_sanity.py` checks filename parity plus obvious dialect drift.
- DONE: Run fresh SQLite migration from empty DB. Evidence: migration sanity script applies all SQLite migrations.
- DONE: Run fresh PostgreSQL migration from empty DB locally. Evidence: latest Docker PostgreSQL 16 disposable container on `127.0.0.1:55436` passed `PARACORD_TEST_POSTGRES_URL=postgres://postgres:postgres@127.0.0.1:55436/paracord_test cargo test -p paracord-db postgres_pool_and_migrations_smoke_when_configured -- --nocapture`, then was stopped cleanly.
- PARTIAL: Run upgrade migration from latest released v0.9.0 DB snapshot. Evidence: latest `python scripts\release_sqlite_upgrade_from_tag_smoke.py v0.9.0` builds a temporary SQLite database from the `v0.9.0` tag migrations, verifies released migration checksums are unchanged, applies 31 current migrations, and records 65 SQLx ledger rows; latest `python scripts\release_postgres_upgrade_from_tag_smoke.py v0.9.0` performed the matching destructive check against a disposable PostgreSQL 16 database on `127.0.0.1:55437` and passed with 35 tag migrations, 31 current migrations applied, and 66 total ledger rows before the container was stopped. Gap: these are synthetic tag-schema upgrades, not real production/user DB snapshot fixtures; representative released SQLite and PostgreSQL user database snapshots are still required before release-grade upgrade validation.
- DONE: Prevent platform checkout line endings from creating accidental SQLx migration checksum drift, script shebang failures, public release-doc churn, and release-workflow CRLF noise. Evidence: `.gitattributes` pins SQLite/PostgreSQL migration SQL, release Python/shell scripts, public release docs, and GitHub workflow YAML to LF; `python scripts/check_migration_line_endings.py` verifies 173 release-critical files resolve to `text eol=lf` and contain no CR bytes in the working tree; and the shell backup/restore script smoke caught and fixed CRLF shebang failures.
- PARTIAL: Test rollback/backup story where rollback is not supported. Evidence: SQLite backup/restore regression covers pre-restore DB copy plus DB/media restore. Local Docker PostgreSQL 16 backup/restore drill now passes: after source migrations and PostgreSQL route smoke, `pg_dump --format=custom --no-owner --no-privileges` restored into a fresh database with expected row counts, then the PostgreSQL migration smoke and route smoke both passed against the restored database. Gap: release-upgrade rollback drill still needs a representative released user database snapshot.
- DONE: Validate migrations do not use SQLite-only syntax in PostgreSQL or vice versa. Evidence: migration sanity dialect drift checks.
- DONE: Run route tests against SQLite. Evidence: API integration tests run against in-memory SQLite.
- PARTIAL: Add broader PostgreSQL route coverage beyond one migration smoke test. Evidence: CI is configured with PostgreSQL migration smoke, PostgreSQL query-plan smoke, and a dedicated `paracord-api` PostgreSQL route smoke using `PARACORD_TEST_POSTGRES_URL`; the route smoke now exercises users, guild creation, channel creation, message create/list, scheduled-message create/list/cancel, role create/list/update/delete, member update/list with role assignment, invite create/preview/list/accept/exhaustion, group DM create/list, reaction add/remove with message-list verification, webhook token execution, economy XP/streak/leaderboard reads, and multipart attachment upload/delete. Latest local Docker PostgreSQL 16 validation from fresh disposable containers passes `cargo test -p paracord-db postgres_pool_and_migrations_smoke_when_configured -- --nocapture`, `cargo test -p paracord-db postgres_query_plan_smoke_when_configured -- --nocapture`, and `cargo test -p paracord-api --test postgres_route_smoke -- --nocapture`. During validation, the shared API test helper was fixed to generate schema-valid <=32 character usernames, the Postgres route smoke now asserts exhausted single-use invites are hidden with 404, group DM recipient inserts were fixed to use portable `ON CONFLICT DO NOTHING`, PostgreSQL `user_xp.last_xp_at` writes/reads were fixed with a typed timestamp cast plus normalized projection, no-nonce message sends now reliably run created-message side effects, and query-plan drift was fixed with hot-path indexes. Gap: the full API route suite still uses the SQLite harness, and CI/GitHub-hosted PostgreSQL still needs to run green before release.

## 6. Feature Completion Audit

- PARTIAL: Verify group E2EE. Evidence: `cargo test -p paracord-api --test coverage_gap_routes -- --quiet` covers sender-key post/get/ack plus acknowledged-envelope retrieval when `since_epoch` is supplied for cache recovery; `cd client && npm run test:unit -- groupDmE2ee.test.ts --reporter=dot` verifies sender-key envelope distribution, recipient decryption, cached follow-up decryption, local-cache-loss recovery from acknowledged envelopes, missing-envelope denial, local sender-key rotation when group membership changes so newly added members cannot decrypt earlier messages, and sender-key rotation when a recipient identity key changes; `cd client && npm run test:unit -- messageStore.test.ts --reporter=dot` covers one-to-one DM locked-key rejection and encrypted payload wiring before API send; `cd client && npm run test:unit -- AccountRecovery.flow.test.tsx --reporter=dot` covers recovery phrase display/acknowledgement, trimmed local identity/recovery usernames, and account recovery form validation/success navigation; one-to-one E2EE DM transport is covered by release product smoke. Gap: full real multi-client group-DM product testing, logout/device reset, and recovery UX still need validation.
- DONE: Verify scheduled messages. Evidence: create/list/cancel route tests, `python scripts/release_product_smoke.py --port 18118`, which schedules a message seven seconds in the future, verifies it appears in the scheduled list, and polls until the release server's background worker delivers it into channel history, plus `python scripts/release_restart_smoke.py --port 18125`, which schedules a pending message before shutdown and verifies the restarted release server delivers it; `cd client && npm run test:unit -- MessageInput.test.tsx --reporter=dot` verifies schedule-mode action labeling, ISO payload wiring, client-side future-time validation before API calls, success reset/toast, and keeping content/date visible when the server rejects the schedule. Editing/rescheduling existing scheduled messages is documented as unsupported in `docs/known-limitations.md`; users should cancel and recreate instead.
- PARTIAL: Verify scheduled events. Evidence: recurrence/reminder/iCal route tests, including explicit clearing of nullable event fields and `entity_type` updates; `python scripts/release_product_smoke.py --port 18119`, which creates and updates an event, verifies RSVP state, exports iCal with weekly recurrence, and polls until the release server's scheduled-event worker sets `reminder_sent_at`, starts the event (`status=2`), and creates the event channel; `python scripts\release_scheduled_events_lifecycle_smoke.py --port 18138`, which verifies member permission denials, unsafe create/update payload rejection, RSVP/iCal, worker start/reminder, auto-created event-channel cleanup on completion, recurrence successor creation, cancellation, and deletion; and `cd client && npm run test:unit -- EventList.test.tsx --reporter=dot`, which verifies manager edit/start/cancel/delete controls, nullable field clearing, event type submission, regular-member RSVP-only behavior, retryable event-load error details, RSVP/status failure details, and encoded path segments for calendar export URLs. Gap: manual event UX pass and longer-run staging recurrence behavior still need broader product validation.
- PARTIAL: Verify onboarding. Evidence: rules gate and role assignment tests, plus `python scripts\release_onboarding_smoke.py --port 18131`, which verifies malformed settings rejection, cross-guild role option rejection, initial incomplete member state, rule-acceptance gate, invalid selected role rejection, valid completion, selected role assignment, and re-entry persistence; `cd client && npm run test:unit -- OnboardingSettingsSection.test.tsx GuildOnboardingGate.test.tsx --reporter=dot` verifies malformed member payload tolerance, configured member gate rendering, admin settings load/edit/save, role option filtering/selection, progressive message threshold clamping, save feedback, and load-error feedback. Gap: mobile onboarding UX still needs manual pass.
- PARTIAL: Verify stickers/animated emoji. Evidence: upload/list/serve/delete tests, plus `python scripts\release_poll_emoji_smoke.py --port 18129`, which verifies release-binary PNG and GIF custom emoji upload, static/animated flag decoding, list membership, served image bytes, and delete; `cargo test -p paracord-api --test coverage_gap_routes custom_emoji_images_require_membership_and_support_query_tokens -- --quiet` verifies custom emoji image auth, member token query fallback, `nosniff`, and served PNG bytes; `cargo test -p paracord-api middleware::tests -- --quiet` verifies query-token fallback is scoped to guild emoji/sticker image assets; `cd client && npm run test:unit -- StickerPicker.test.tsx EmojiPicker.serverEmoji.test.tsx MessageInput.sticker.test.tsx EmojisSection.test.tsx --reporter=dot` covers sticker loading/filtering/select/retryable error/empty states, sticker-only composer send, sticker/GIF quick-send API error details, server custom-emoji loading/filtering/animated-token insertion, gateway refresh, guild settings emoji upload validation/rename/delete/read-only/empty states; and `cd client && npm run test:unit -- customEmoji.test.ts --reporter=dot` covers token-aware cross-origin custom emoji image URLs. Gap: full manual visual picker/upload-management pass still needs product validation.
- PARTIAL: Verify moderation templates. Evidence: create/apply/delete/timed mute tests, plus `python scripts\release_moderation_templates_smoke.py --port 18132`, which verifies release-binary permission denials, malformed template rejection, timed mute application, member timeout persistence, audit-log reason/target metadata, template change metadata, and deletion; `cd client && npm run test:unit -- ModerationTemplatesSection.test.tsx --reporter=dot` verifies timed-mute template creation/reset, disabled create state, existing template rendering, target-user apply with optional reason/DM overrides, success feedback, named delete action, and empty state. Gap: manual visual/product pass for moderation-template workflows remains.
- PARTIAL: Verify economy/progression. Evidence: XP, level roles, leaderboard, achievements tested, plus `python scripts\release_economy_smoke.py --port 18134`, which verifies release-binary level-role permission denial, malformed/cross-guild mapping rejection, valid mapping persistence, first-message XP and achievement award, streak/rank state, cooldown anti-spam preventing rapid XP farming, leaderboard membership/ranks, and level-role auto-assignment for multiple users; `cd client && npm run test:unit -- GuildEconomyPanel.test.tsx EconomySettingsSection.test.tsx --reporter=dot` verifies current progress, achievements, leaderboard rendering, load-error feedback, level-role mapping load/add/remove/save, filtered assignable roles, and save toast feedback. Gap: longer-run progression tuning and manual visual/product pass remain.
- PARTIAL: Verify templates. Evidence: `python scripts/release_product_smoke.py --port 18124` creates a guild template from a live guild, verifies the template appears in the list, rejects unsafe `<script>` markup when applying a template, applies the template to create a new guild, verifies source text/voice/announcement/forum channels are recreated, and deletes the template; `cargo test -p paracord-api --test coverage_gap_routes guild_template_apply_rejects_malicious_stored_data_without_partial_guild -- --quiet` covers the route regression and verifies Paracord forum channel type `7` survives template apply; `python scripts\release_template_safety_smoke.py --port 18136` verifies stored invalid JSON, unsafe role names, invalid role permissions, unsafe channel names, and invalid channel types are rejected before partial guild creation, while safe stored role/channel data still applies and increments usage count; `node client\scripts\release-real-ui-smoke.mjs --port 18152` now drives the production UI through Template Gallery source-guild selection, template creation, template preview, and create-from-template navigation while verifying the API usage count increments; `cd client && npm run test:unit -- TemplateGalleryPage.test.tsx --reporter=dot` now passes 5 tests covering template browsing/details, source-guild create, apply-to-new-server navigation and channel fetch, valid selection after deleting the selected template, owner delete confirmation, concrete apply/create/delete error feedback, and load-error feedback; and `cd client && npm run test:unit -- CreateGuildModal.template.test.tsx --reporter=dot` covers create-server create/join/template tabs, template preview, apply-to-new-server navigation/channel selection, invite-code parsing/navigation, and announced template-load error feedback with concrete API details. Gap: manual visual/product pass for the template gallery and create-server template tab remains.
- PARTIAL: Verify bot store metrics/reviews. Evidence: install/review/metrics route tests, plus `python scripts\release_bot_store_smoke.py --port 18133`, which verifies private bots are hidden from review endpoints, seeded public bots appear in store search/categories/featured, invalid ratings and overlong titles are rejected, review upsert does not duplicate, multiple-review averages are correct, metrics are owner-only, install/uninstall counts update, and metric event buckets include review/install/uninstall events; `cd client && npm run test:unit -- BotAuthorizePage.reviews.test.tsx BotStoreCard.test.tsx BotStoreSection.test.tsx DeveloperPage.metrics.test.tsx CommandBuilder.test.tsx --reporter=dot` verifies review summary/list rendering, rating/body submission, review refresh, review/load/authorization API error details, bot authorization from the review surface, public store-card metadata/rating/tag rendering, install action, disabled install state, adding state, safe icon rendering, unresolved icon-hash fallback, retryable public bot-store load errors, public bot add failure details, built-in bot install failure details, labeled/trimming create-bot form behavior, developer metrics refresh, install/active-guild/review/event-bucket rendering, graceful metrics-load failure, token-copy failure details, create-bot API error details, command create/update API error details, and accessible command-builder option controls. Gap: manual visual pass for full public store browsing/search and developer metrics remains.
- PARTIAL: Verify anonymous posting/disappearing messages. Evidence: route enforcement tests, `python scripts/release_product_smoke.py --port 18121`, which enables a one-second disappearing-message policy in a live channel, sends a message, and polls until the release server's disappearing-message worker removes it from channel history, plus `python scripts\release_channel_features_smoke.py --port 18135`, which verifies release-binary anonymous author hiding for normal members, admin deanonymize metadata, deanonymize permission denial/allow, `expires_at` output, and disappearing-message worker cleanup; `cd client && npm run test:unit -- MessageList.anonymous.test.tsx MessageInput.test.tsx --reporter=dot` verifies anonymous message badge rendering, disappearing-message expiry label rendering, visible reaction/pin failure feedback in message history, the composer warning when channel feature settings enable anonymous posting, slowmode/rate-limit send-error feedback, and composer attachment upload behavior. Gap: manual audit UX and broader retention edge cases remain.
- PARTIAL: Verify slowmode/adaptive slowmode. Evidence: server-side route enforcement tests plus `python scripts\release_channel_features_smoke.py --port 18135`, which verifies release-binary adaptive message slowmode and thread slowmode return 429 for rapid repeat actions while feature-setting validation and permission checks hold; `cd client && npm run test:unit -- MessageInput.test.tsx --reporter=dot` verifies rate-limited/slowmode send failures display the server error in the composer. Gap: manual edge cases remain.
- PARTIAL: Verify native media. Evidence: route behavior, package build with VPX, and `python scripts/release_product_smoke.py --port 18117`, which starts native media on a per-run UDP port and verifies native voice join/leave plus native stream start/stop response shape; native guild/DM voice joins now fail if the revocation voice-state row cannot be written, clean up that row if native room admission fails, and `cargo test -p paracord-api --test voice_routes -- --quiet` covers native DM voice token/voice-state behavior, targeted DM-recipient `VOICE_STATE_UPDATE` dispatch for join and leave, silent no-op leave for non-active DM channels, DM voice leave authorization/channel scoping, and guild voice session handling. Gap: real multi-client audio, desktop screen capture, stream viewing, simulcast, reconnect, system-audio capture, and packet-loss testing remain.
- DONE: Verify LiveKit fallback. Evidence: voice route tests cover fallback routing/shape, and `python scripts\federation_live_fundamentals_validation.py` starts the bundled managed LiveKit server and verifies LiveKit voice join plus LiveKit stream start/stop response shape with realtime `VOICE_STATE_UPDATE` gateway assertions.

## 7. UI/UX Fixes

- DONE: Fix broken channel settings edit route or gear behavior. Evidence: the channel gear opens the settings overlay directly to `Channels` with target channel highlighting/scrolling, and `cd client && npm run test:e2e` verifies the edit gear opens the `Server settings` dialog, lands on the `Channels` heading, highlights the edited channel, and closes with Escape.
- DONE: Add invalid channel route handling verification. Evidence: `cd client && npm run test:e2e` now navigates to an invalid channel route and verifies the `Channel not found` heading.
- DONE: Add client-side admin access guard before admin panels. Evidence: `cd client && npm run test:e2e` verifies a non-admin user navigating to `/app/admin` sees the `Access denied` guard without calling `/api/v1/admin/stats`, and release-server product smoke verifies the API rejects second-user admin access.
- PARTIAL: Standardize modal accessibility. Evidence: settings overlays, admin edit-guild modal, admin destructive confirmations, forum tag manager/new-post dialogs, MessageList create-thread/report-message dialogs, FilePreview image lightbox, CreateGuildModal, CommandPalette, DM picker, channel permissions editor, Sidebar create-folder dialog, and user report/identity verification dialogs now use app dialogs/confirmation flows instead of native browser confirm prompts where applicable; `cd client && npm run test:a11y:static` now verifies icon-only button names, `aria-modal` dialog role/name metadata, focusable `tabIndex={-1}` dialog containers, literal `aria-labelledby` target IDs, native string `confirm()` usage in React UI, and non-interactive click handlers, and passes after the admin security-event pagination/details UI; `cd client && npm run test:unit -- useFocusTrap.test.tsx --reporter=dot` covers initial focus, Tab/Shift+Tab wrapping, Escape close, and focus restore; `cd client && npm run test:unit -- Sidebar.test.tsx --reporter=dot` covers the Sidebar create-folder dialog semantics, initial focus, and Escape close path; `cd client && npm run test:unit -- FilePreview.test.tsx FileUpload.test.tsx --reporter=dot` covers keyboard-open image preview, dialog semantics, initial close-button focus, Escape close, and named upload/remove controls; `cd client && npm run test:unit -- CreateGuildModal.template.test.tsx CommandPalette.test.tsx --reporter=dot` covers CreateGuildModal create/join/template controls and CommandPalette Ctrl+K open, dialog metadata, explicit search label, initial focus, Tab containment, Escape close, and focus restore; and `cd client && npm run typecheck` plus `cd client && npm run test:unit -- --reporter=dot` pass. Gap: full screen-reader/manual focus-order audit remains.
- DONE: Add accessible names to icon-only buttons. Evidence: `cd client && npm run test:a11y:static` passes after adding explicit labels to the flagged forum, custom CSS, developer command builder, file preview, invite, server hub, sidebar, voice, invite-page, user-profile, friends, channel-permission, GIF, sticker, and user-panel controls; admin federation/storage settings, bot-store configuration controls, command-builder inputs, and economy level-role mapping controls now expose explicit programmatic labels while preserving the existing visual labels; the static gate fails on future icon-only buttons without accessible names and native string `confirm()` prompts in React UI.
- DONE: Ensure tooltips are not the only accessible label. Evidence: `cd client && npm run test:a11y:static` now requires icon-only buttons to use `aria-label`/`aria-labelledby` rather than counting `title`, fails title-only `icon-btn`, `command-icon-btn`, and `hover-action-btn` controls, and fails Tooltip-wrapped buttons/role-buttons without an explicit accessible name. It caught and fixed stream viewer preview/maximize/stop controls plus user-profile, friends, channel-permission, compact navigation/sidebar, top-bar, voice-control, Markdown toolbar, GIF, and sticker controls.
- PARTIAL: Keyboard-test core flows. Evidence: `cd client && npm run test:e2e` passes with keyboard Enter message send, command-palette Ctrl+K open/input focus/Escape close/filter/Enter selection, keyboard focus/Enter/Space activation of text and voice channel tree items, and ArrowUp/ArrowDown channel-tree focus movement through visible category/channel treeitems; `cd client && npm run test:unit -- ContextMenu.test.tsx --reporter=dot` now covers menu focus on mount, arrow-key highlighting with `aria-activedescendant`, Enter/Space activation, and disabled/divider skipping; `cd client && npm run test:unit -- Sidebar.test.tsx --reporter=dot` covers server context-menu keyboard open, main-menu Arrow/Home/End focus movement, folder-submenu Arrow/Home/End focus movement, and Escape/ArrowLeft return to the submenu trigger; `cd client && npm run test:unit -- UserPanel.test.tsx --reporter=dot` covers the user identity control as a named, focusable button for username copy; `cd client && npm run test:unit -- Button.test.tsx --reporter=dot` covers the shared Button defaulting to `type="button"` while preserving explicit submit buttons; `cd client && npm run test:unit -- CommandPalette.test.tsx --reporter=dot` covers Ctrl+K open, Tab containment, Escape close, and focus restore; `cd client && npm run test:a11y:static` now fails non-interactive clickable elements outside inert backdrop/stop-propagation containers, which caught and fixed the UserPanel identity area, FilePreview thumbnail, message author/edit-history controls, recent-DM row, and guild-hub channel row; channel/member copy-ID menus and the server dock context menu now open from Shift+F10/ContextMenu, expose menu/menuitem roles, focus an action, and close on Escape; the server dock folder submenu now opens from keyboard, moves focus with Arrow/Home/End, and closes back to the submenu trigger with Escape/ArrowLeft. Gap: full app-wide keyboard traversal, every modal focus order, and screen-reader workflow still need an accessibility pass.
- PARTIAL: Mobile-test widths 320, 375, 414, 768. Evidence: real UI smoke nonblank dashboard at these widths, and `cd client && npm run test:e2e` now asserts core mocked app rendering plus no document-level horizontal overflow at those widths with long names. Gap: full workflow/mobile interaction pass remains.
- PARTIAL: Desktop-test 1366x768, 1440x900, 1920x1080. Evidence: real UI smoke includes 1366 and 1920 body checks, and `cd client && npm run test:e2e` now asserts the mocked app's core channel view is visible with no document-level horizontal overflow at 1366x768, 1440x900, and 1920x1080. Gap: full manual workflow pass across desktop surfaces remains.
- PARTIAL: Check long server/channel/user/message names for overflow. Evidence: `cd client && npm run test:e2e` now uses long guild, channel, username, and unbroken message content, then asserts no document-level horizontal overflow at 320, 375, 414, and 768px. Gap: broader manual visual review of all surfaces remains.
- PARTIAL: Check empty/loading/error states for every major page. Evidence: `docs/release-empty-loading-error-inventory.md` maps every major page and shared surface to current empty/loading/error behavior and identifies remaining gaps; `cd client && npm run test:e2e` verifies invalid-channel not-found, voice-unavailable state, empty-state/lazy-route rendering for `/app`, friends, DMs, discovery, templates, and developer portal, and channel-tree keyboard state; unit tests cover several component/store error paths; `cd client && npm run test:unit -- HomePage.test.tsx FriendsPage.test.tsx DMList.test.tsx DMPage.test.tsx TopBar.dm.test.tsx CreateGuildModal.template.test.tsx MessageInput.test.tsx MessageInput.sticker.test.tsx FileUpload.test.tsx AccountRecovery.flow.test.tsx AccountUnlockPage.test.tsx RegisterPage.test.tsx InvitePage.test.tsx DiscoveryPage.test.tsx ChannelManager.test.tsx ChannelPermissionsEditor.test.tsx CommandBuilder.test.tsx --reporter=dot` verifies dashboard online-friend DM success/failure feedback, friend action inline errors/pending guards/accessibility labels, single-DM create, group-DM create, group-member add/remove failures, DM voice-call join failure feedback, no-eligible-friends states, create-server create/join/template flows, template-load errors, registration password-confirmation blocking and success navigation, InvitePage preview failure/login/accept states, DiscoveryPage load-error retry/join/no-public-invite states, channel-admin API error details, announcement-follow failure feedback, permission-overwrite API error details, command-builder create/update API error details, slowmode/send/schedule/sticker/GIF errors, selected-file upload wiring, upload-control empty/input states, recovery phrase acknowledgement, trimmed account setup/recovery names, invalid recovery phrase rejection, successful recovery navigation, unlock redirect, failed-attempt cooldown, and unlock reconnect behavior show user-visible feedback. Gap: manual visual QA with empty datasets, failed network calls, and long names remains.
- PARTIAL: Check dark/light/AMOLED themes. Evidence: theme code/contrast test plus `cd client && npm run test:e2e`, which now has the mocked server return `dark`, `light`, `amoled`, and `high-contrast` settings, verifies `document.documentElement[data-theme]` updates for each, and checks no document-level horizontal overflow. Gap: manual visual pass remains.
- DONE: Verify custom CSS cannot break security assumptions. Evidence: `sanitizeCustomCss` now preserves theme custom properties while stripping `display`, `visibility`, `opacity`, `filter`, positioning, z-index, `pointer-events`, `transform`, `clip-path`, `backdrop-filter`, `cursor`, `url()`, `expression()`, `javascript:`, `@import`, nested `@media`/`@supports` at-rule blocks, and `-moz-binding`; server profile routes and client `safeExternalUrl` block scriptable, relative, oversized, and userinfo-bearing external URLs used by bot link buttons, bot OAuth redirects, message embeds, GIF picks, GitHub webhook cards, update notices, ErrorBoundary bug-report links, profile linked accounts, and Markdown autolinks; `safeClientResourceUrl` blocks unsafe attachment preview/download targets, message embed images, image lightbox resources, sticker images, message sticker/attachment images, screen-share thumbnails, blob URLs, and scriptable/protocol-relative resource URLs while allowing same-origin paths and HTTP(S) resources; stored image fields now use `safeStoredImageDataUrl` so guild hub banners, server hub banner uploads/previews, guild settings icon saves, sidebar/home/discovery/welcome guild icons, profile banners, bot-store icons, pinned-message author avatars, and entity-select avatars render only safe raster data URLs and fall back instead of requesting missing legacy image endpoints or external avatar URLs; selected-file previews, FilePreview inline image rendering, and MessageList inline image attachments now use the shared safe raster-image MIME allowlist instead of broad `image/*`; custom emoji image URLs use token-aware resource URLs for cross-origin browser image loads, and guild emoji/sticker image query-token fallback is scoped to those image asset routes; `cargo test -p paracord-api --test coverage_gap_routes profile_fields_include_pronouns_and_linked_accounts -- --quiet` covers profile linked-account URL filtering, `cargo test -p paracord-api --test coverage_gap_routes custom_emoji_images_require_membership_and_support_query_tokens -- --quiet` covers custom emoji image auth/query-token/`nosniff`, `cd client && npm run test:unit -- customEmoji.test.ts --reporter=dot` covers token-aware custom emoji image URLs, `cd client && npm run test:unit -- security.test.ts BotStoreCard.test.tsx MessageComponents.test.tsx GuildWelcomeScreen.test.tsx GuildHub.test.tsx Sidebar.test.tsx DiscoveryPage.test.tsx HomePage.test.tsx --reporter=dot` covers safe stored image rendering/fallback, `cd client && npm run test:unit -- ScreenSharePickerModal.test.tsx BotAuthorizePage.reviews.test.tsx MessageEmbed.test.tsx GifPicker.test.tsx StickerPicker.test.tsx MessageList.anonymous.test.tsx security.test.ts --reporter=dot` passes 53 targeted URL/resource safety and related tests, `cd client && npm run test:unit -- markdown.test.ts security.test.ts --reporter=dot` covers safe/inert Markdown autolinks, `cd client && npm run test:unit -- ImageLightbox.test.tsx TopBar.pins.test.tsx security.test.ts --reporter=dot` covers unsafe lightbox and pinned-avatar fallback behavior, `cd client && npm run test:unit -- security.test.ts TopBar.pins.test.tsx ErrorBoundary.test.tsx --reporter=dot` covers the stricter resource URL gate, external pinned-avatar fallback, and bug-report URL fallback, `cd client && npm run test:unit -- FileUpload.test.tsx FilePreview.test.tsx MessageInput.test.tsx security.test.ts --reporter=dot` covers unsafe selected-file and attachment preview image MIME fallback behavior, and `cd client && npm run test:unit -- --reporter=dot` passes 91 files / 513 tests.

## 8. Manual End-To-End Product Pass

- PARTIAL: Fresh install/run server. Evidence: release server binary smoke passes. Gap: clean-machine install not done.
- DONE: Register first user and confirm admin promotion. Evidence: real server smoke plus `cd client && npm run test:unit -- RegisterPage.test.tsx --reporter=dot`, which verifies password-confirmation mismatch blocking, trimmed registration payloads, connected-server persistence, app navigation, and unlocked local public-key attachment.
- DONE: Register second user and confirm non-admin restrictions. Evidence: `python scripts/release_product_smoke.py --port 18099` registered a second user and verified `/api/v1/admin/stats` rejects that token.
- DONE: Create guild. Evidence: real server smoke plus `cd client && npm run test:unit -- CreateGuildModal.template.test.tsx --reporter=dot`, which verifies the create-server modal create tab calls guild creation, fetches channels, selects the first channel, navigates, and closes.
- DONE: Create text, voice, forum, announcement/thread channels. Evidence: `python scripts/release_product_smoke.py --port 18099` created text, voice, announcement, and forum channels, then created/listed a thread.
- DONE: Send/edit/delete/reply/pin/react messages. Evidence: `python scripts/release_product_smoke.py --port 18099` covered send, reply, edit, pin/list pins, reaction add/remove, search, unpin, and delete.
- DONE: Upload files/images and verify embeds/lightbox. Evidence: `python scripts/release_product_smoke.py --port 18112` uploaded a PNG through the rebuilt release server, attached it to a message, verified message attachment metadata, and downloaded the same bytes under auth; `node client/scripts/release-real-ui-smoke.mjs --port 18152` starts the release server, logs in through the real browser UI, navigates through the app shell to a real channel, sends a message, uploads a PNG through the composer, verifies the image attachment in message history, opens the shared image viewer, closes it with Escape, opens/closes the channel settings dialog, opens admin settings, exercises user/guild/federation-peer delete confirmations, backup create/download/restore/delete, verifies security-event pagination/details, and observes no page errors; file/sticker route tests also pass; `cd client && npm run test:unit -- MessageInput.test.tsx FilePreview.test.tsx FileUpload.test.tsx --reporter=dot` covers selected-file image preview rendering, attachment upload, attachment ID send wiring, keyboard-open image preview, dialog semantics, initial close-button focus, Escape close, named upload/remove controls, hidden input selection forwarding, and unsafe image MIME fallback.
- DONE: Search messages product pass. Evidence: `python scripts/release_product_smoke.py --port 18099` found an edited message through `/api/v1/channels/{channel_id}/messages/search`; `cd client && npm run test:unit -- SearchPanel.test.tsx SearchOverlay.test.tsx --reporter=dot` verifies labeled search/filter controls, named close actions, global overlay progress status, and announced search-unavailable feedback when both server search and fallback recent-message filtering fail.
- DONE: Create roles and channel overwrites API product pass. Evidence: `python scripts/release_product_smoke.py --port 18099` created/listed a role and created/listed a channel overwrite.
- DONE: Invite user and accept invite product pass. Evidence: `python scripts/release_product_smoke.py --port 18099` created, previewed, and accepted an invite with the second user, then verified membership; `cd client && npm run test:unit -- CreateGuildModal.template.test.tsx --reporter=dot` verifies the join tab accepts invite URLs by code, adds the joined guild, fetches/selects its first channel, navigates, and closes; `cd client && npm run test:unit -- InvitePage.test.tsx --reporter=dot` verifies invite preview failure feedback, unauthenticated accept routing to login, authenticated accept payload with verification acknowledgement/answers, guild insertion, channel fetch/selection, and navigation.
- DONE: DM and group DM API product pass. Evidence: `python scripts/release_product_smoke.py --port 18099` created a one-to-one DM and a group DM, then verified group recipients.
- PARTIAL: Enable/use E2EE DM product pass. Evidence: `python scripts/release_product_smoke.py --port 18113` verifies plaintext one-to-one DM rejection, encrypted v2 DM send, no plaintext content in the response, exact `e2ee` nonce/ciphertext/header metadata, and recipient message listing; `cd client && npm run test:unit -- messageStore.test.ts --reporter=dot` verifies the client message store refuses one-to-one encrypted sends while the account key is locked and sends empty plaintext plus the v2 encrypted payload when unlocked; `cd client && npm run test:unit -- AccountRecovery.flow.test.tsx --reporter=dot` verifies local identity recovery phrase acknowledgement, trimmed setup/recovery names, and recovery form validation/success navigation. Gap: real client key generation/decryption UX and device recovery still need manual multi-client validation.
- PARTIAL: Join voice product pass. Evidence: `python scripts/release_product_smoke.py --port 18117` enables native media through env config, joins the voice channel, verifies `native_media=true`, `media_endpoint`, `media_token`, `room_name`, and `session_id`, starts/stops a native stream, then leaves with the returned session id; `python scripts\federation_live_fundamentals_validation.py` also verifies LiveKit voice join with the bundled managed LiveKit server and realtime `VOICE_STATE_UPDATE`; `cargo test -p paracord-api --test voice_routes -- --quiet` verifies native DM voice join and active leave emit targeted recipient-scoped `VOICE_STATE_UPDATE` events while no-op leaves for other DMs stay silent; `cd client && npm run test:unit -- TopBar.dm.test.tsx --reporter=dot` verifies direct-message voice-call join failures surface a user-visible error toast. Gap: real desktop/browser audio capture/playback remains untested.
- PARTIAL: Share screen with and without system audio manual/product pass. Evidence: `python scripts/release_product_smoke.py --port 18117` starts and stops the native stream API with `quality_preset=720p30`. Gap: this does not exercise real desktop capture, system audio selection, encoder output, or receiving clients.
- PARTIAL: Watch another stream manual/product pass. Evidence: `cd client && npm run test:unit -- --reporter=dot` passes with `VoiceParticipants.test.tsx`, which verifies a live participant's `Watch ... stream` control has an accessible label, sets `watchedStreamerId`, and navigates to the voice channel. Gap: real two-client media publish/subscribe, audio/video decode, and stream rendering still require manual product validation.
- PARTIAL: Reconnect after server restart manual/product pass. Evidence: `python scripts/release_restart_smoke.py --port 18116` creates guild/channel/message state, stops the release server, restarts against the same SQLite DB, lists the pre-restart message with the existing access token, refreshes the persisted session, and sends a post-restart message; `python scripts\release_gateway_resume_smoke.py --port 18139` verifies the release gateway accepts a same-session resume with an older sequence, sends RESUMED, replays the buffered `MESSAGE_CREATE`, and falls back to fresh READY for an unknown resume session; `cd client && npm run test:unit -- connectionManager.test.ts --reporter=dot` verifies the client treats RESUMED as a successful lifecycle event by resetting reconnect attempts and flushing queued outbound presence/voice messages. Gap: packaged desktop UI reconnection after a full server process restart still needs multi-client product validation.
- DONE: Test logout/relogin/session refresh API product pass. Evidence: `python scripts/release_product_smoke.py --port 18112` verifies session listing, refresh-token rotation, old access-token rejection after refresh, password login creating a second session, logout revoking that session, logged-out access-token rejection, logged-out refresh-token rejection, and continued refreshability of the primary session; `cargo test -p paracord-api routes::auth::tests::csrf_cookie_is_readable_from_app_routes -- --quiet` verifies the CSRF cookie is scoped so app routes can read it for refresh requests; and `node client\scripts\release-real-ui-smoke.mjs --port 18152` now hard-loads `/app/templates` after login and verifies the authenticated app route is restored instead of bouncing to `/login`.
- PARTIAL: Test password reset/MFA if included. Evidence: `python scripts/release_product_smoke.py --port 18117` verifies forgot-password existing/missing account responses match, invalid reset tokens are rejected, MFA setup/status works, invalid setup code is rejected, valid TOTP setup enables MFA and returns 10 backup codes, password login requires an MFA ticket, MFA login issues tokens, and a backup code can disable MFA; `cargo test -p paracord-api --test security_federation_regressions password_reset_completion_updates_password_revokes_sessions_and_consumes_token -- --quiet` verifies successful reset-token completion, one-time token use, old-session revocation, old-password rejection, and new-password login; `cd client && npm run test:unit -- LoginPage.flow.test.tsx --reporter=dot` verifies password reset request/token submission, mismatched-password blocking, MFA challenge display, MFA code submission, navigation after MFA login, and rotated token adoption after public-key attachment during login; `cd client && npm run test:unit -- UserSettings.mfa.test.tsx --reporter=dot` verifies account-settings MFA setup, backup-code display, disable-with-code flow, MFA status/setup/verify/disable failure details, profile-save failure details, and settings-save failure details. Gap: real SMTP password-reset delivery and manual client UX pass remain.
- PARTIAL: Test server settings/admin dashboard. Evidence: `python scripts/release_product_smoke.py --port 18123` verifies admin stats, admin settings read/update/reload, runtime setting persistence, config-level federation cache setting persistence, and non-admin denial for stats/settings; `node client/scripts/release-real-ui-smoke.mjs --port 18152` verifies the real browser can open the channel rail settings action, render the Server settings dialog on the Channels section, open the admin dashboard, check visited admin tabs for document-level horizontal overflow, delete a disposable user and guild through confirmation dialogs, add/inspect/remove a federated peer through the admin UI, edit every visible admin settings field, verify the saved settings through the API and after remounting the Settings panel, create/download/restore/delete a backup, page through security events, filter backup create/restore/delete events, and expand backup-create details against a real release server; `cargo test -p paracord-api --test coverage_gap_routes admin_settings_update_returns_full_settings_payload -- --quiet` verifies the admin settings PATCH response returns the complete load-compatible settings shape; `cd client && npm run test:unit -- UserPanel.test.tsx GuildHub.test.tsx LoginPage.flow.test.tsx --reporter=dot` covers admin dashboard control visibility, scheme-less GuildHub server URL rendering, and login auth-store/token refresh; `cargo test -p paracord-api --test coverage_gap_routes admin_backup_routes_reject_header_unsafe_filenames -- --quiet` verifies backup download/delete/restore reject header-unsafe filenames instead of panicking or building unsafe response headers. Gap: full manual visual admin dashboard UX still needs product validation.
- DONE: Test public discovery if enabled. Evidence: `cargo test -p paracord-db test_create_guild_with_valid_data -- --quiet` verifies new guild rows are private by default, `python scripts\release_discovery_smoke.py --port 18130` verifies new guilds are absent from public discovery, non-members cannot publish a guild, owners can publish with normalized/deduped discovery tags, published guilds appear in default/search/tag discovery queries, owners can unpublish, and invalid tags are rejected, `cargo test -p paracord-api --test coverage_gap_routes public_guild_invites_are_visible_to_discovery_joiners -- --quiet` verifies private guild invite lists remain manager-only while public guild invite lists expose usable invite codes to authenticated discovery joiners, `node client\scripts\release-real-ui-smoke.mjs --port 18152` verifies a non-member joins a public discovered guild through the production UI, and `cd client && npm run test:unit -- DiscoveryPage.test.tsx --reporter=dot` verifies discovery load-error retry, public-invite join navigation, no-public-invite feedback, and an accessible back action.
- PARTIAL: Test federation basics if advertised across live nodes. Evidence: `python scripts/federation_e2e_validation.py` passes for three local nodes, message create/edit/delete, reaction add/remove, member join/leave, and A-to-B-to-C relay without direct A-to-C trust; `python scripts\federation_live_fundamentals_validation.py` now also passes for three local live nodes with gateway realtime assertions, threads, polls, custom emojis, friends, DMs, user settings, LiveKit voice/stream response checks, and A-origin event propagation to C through B. Gap: externally deployed/staging federation validation is still required before a public release.
- DONE: Test bot creation/OAuth/install/command interaction at release API level. Evidence: bot system route tests, including unsafe component URL rejection for interaction callbacks and followups plus persistence of interaction-response, original-response edit, and followup components through channel message history; `cargo test -p paracord-db -- --quiet` verifies the shared message row layer; SDK tests pass; and `python scripts/release_product_smoke.py --port 18124` creates a bot application, verifies token/bot user output, OAuth-installs it into a live guild, verifies guild bot listing, creates global and guild commands, verifies guild command discovery, invokes a slash command, posts a token-based callback with components, invokes the component interaction, creates a followup message, removes the bot, and verifies it disappears from the guild bot list.
- DONE: Test webhooks including GitHub secret behavior at API level. Evidence: `python scripts/release_product_smoke.py --port 18099` created a webhook, executed/edited/deleted a webhook message, rejected an invalid GitHub HMAC signature, and accepted a valid GitHub HMAC signature.

## 9. Platform And Packaging

- DONE: Build release server binary. Evidence: `cargo build --release --bin paracord-server` passed after the latest production client build.
- DONE: Build Tauri Windows installer. Evidence: latest `cd client && npm run tauri -- build --bundles msi` passes on 2026-05-18 with `VPX_LIB_DIR`, `VPX_INCLUDE_DIR`, `VPX_VERSION=1.16.0`, and `VPX_STATIC=1` set to the local static vcpkg libvpx install; the build recompiles `env-libvpx-sys`, `vpx-encode`, `paracord-codec`, and `paracord-desktop`, then produces `target\release\bundle\msi\Paracord_0.9.0_x64_en-US.msi` (9,076,736 bytes). Latest Inno Setup 6.4.2 validation also passes on 2026-05-18 and produces `installer\output\Paracord-Setup-0.9.0.exe` (9,349,685 bytes).
- BLOCKED: Build Tauri Linux package locally. Evidence: WSL reports an Ubuntu distro, but launching it fails with `Wsl/Service/CreateInstance/MountDisk/HCS/ERROR_FILE_NOT_FOUND` because the distro `ext4.vhdx` is missing. Gap: no Linux package built here; run the GitHub Linux release job or a clean Linux builder.
- BLOCKED: Validate packaged app starts on clean machine/VM. Gap: no clean VM run.
- DONE: Validate bundled UI is present in server release binary. Evidence: `cargo build --release --bin paracord-server` passed after the latest client build, and `python scripts/release_embedded_ui_smoke.py --port 18142` against the rebuilt release server returned HTML with `id="root"`, current `assets/index-BXkAOfWY.js`, hashed JS assets, and no blocked inline service-worker cleanup script.
- DONE: Validate LiveKit bundling/startup behavior. Evidence: `python scripts\federation_live_fundamentals_validation.py` uses `dist\paracord-server\livekit-server.exe`, the managed server starts on port 27880, the admin API health check passes, and LiveKit voice/stream response checks pass.
- DONE: Validate VPX/libvpx setup on Windows. Evidence: latest Windows Tauri MSI build succeeded with the static vcpkg libvpx paths explicitly set and rebuilt `env-libvpx-sys`/`vpx-encode`.
- DONE: Resolve docs mismatch: AGENTS used `VPX_DYNAMIC=1`; docs now use `VPX_STATIC=1` to match static vcpkg config and CI.
- PARTIAL: Confirm macOS support claims. Evidence: known limitations docs and Tauri identifier now uses `com.paracord.desktop` instead of the previous `.app` suffix. Gap: macOS build/smoke not run.
- PARTIAL: Confirm Windows screen/audio capture works. Evidence: Windows package build. Gap: real capture pass not run.
- PARTIAL: Confirm Linux capture behavior works or document limitations. Evidence: limitations docs. Gap: Linux runtime pass not run.
- PARTIAL: Confirm auto-updater config/signing if advertised. Evidence: docs softened and CI gates updater artifacts on signing key. Gap: signed release workflow not run.
- BLOCKED: Confirm installer uninstall/update path. Gap: requires a clean Windows install/update/uninstall environment or VM.

## 10. Docs And Release Notes

- DONE: Update README so claims match actual support.
- DONE: Remove or soften unsupported claims around macOS audio, federation parity, native media completeness, no-default support, and README streaming/system-audio claims. Evidence: README live streaming now matches the four server quality presets (`720p30`, `1080p60`, `1440p60`, `4k60`) and describes native system-audio capture as platform-specific release-validation work rather than guaranteed production behavior.
- DONE: Update self-hosting guide.
- DONE: Update Docker docs. Evidence: latest `docker compose config` passes; latest `docker build -t paracord:release-readiness-smoke .` rebuilds the Linux server image successfully with current client production assets embedded; `docker run -d --rm --name paracord-docker-smoke-20260518 -p 18150:8090 ... paracord:release-readiness-smoke` answered `/health` with `service=paracord status=ok` before clean `docker stop`; and `paracord-docker-ui-smoke-20260518` served embedded HTML with `id="root"` plus hashed `/assets/index-B3p8LFcE.js` before clean `docker stop`. Full `docker compose up -d --build` initially found a real restart-loop caused by placeholder `devkey/devsecret` LiveKit credentials; Compose now uses non-placeholder matching local credentials, pins LiveKit to `livekit/livekit-server:v1.9.11`, starts Paracord plus LiveKit, applies migrations, passes the LiveKit admin health check, answers `GET /health`, and cleans up with `docker compose down --volumes --remove-orphans`.
- DONE: Update security hardening checklist. Evidence: security docs/tracker/gate present.
- DONE: Update bot development docs if SDK/API changed. Evidence: `docs/bot-development.md` now documents application creation/install, command registration, guild command discovery, interaction callback/followup/original-response endpoints, TypeScript SDK usage, and default local URLs; `packages/paracord-bot-sdk/README.md` example now uses the default `8090` server port and calls `.build()` on `SlashCommandBuilder`; latest SDK `npm ci`, `npm run build`, `npm test`, Node integration tests, audit, and dry-pack checks pass.
- DONE: Update API contracts for new endpoints.
- PARTIAL: Add migration/upgrade notes from v0.9.0 with real snapshot evidence. Evidence: `docs/known-limitations.md` documents backup/no-rollback posture, latest `python scripts\release_sqlite_upgrade_from_tag_smoke.py v0.9.0` provides synthetic tag-schema SQLite upgrade evidence, and latest `python scripts\release_postgres_upgrade_from_tag_smoke.py v0.9.0` provides synthetic tag-schema PostgreSQL upgrade evidence. Gap: obtain real released v0.9.0 user DB snapshots and run SQLite plus PostgreSQL upgrade validation before treating this as release-grade.
- DONE: Add known limitations section.
- DONE: Add release notes grouped by features, security, fixes, breaking changes, migration notes.
- DONE: Ensure screenshots are current and not misleading. Evidence: `docs/release-screenshot-inventory.md` inventories public README screenshots and repo-local image assets; `client/ui_mock_screenshot.png` is blank/white and not release-suitable; `cd client && npm run screenshots:release` captures current production-client dashboard/text-chat candidates under `docs/screenshots/`; pixel sampling verifies both 1440x900 PNGs are nonblank; README now embeds the current local text-chat screenshot and removes the old GitHub-hosted voice/streaming screenshots until real media validation passes.

## 11. Security/Abuse Validation

- DONE: Rate-limit login, register, refresh, password reset. Evidence: rate-limit regression/security tracker.
- DONE: Verify generic password reset responses. Evidence: auth route coverage and security tracker.
- DONE: Verify CSRF for cookie-auth mutating requests. Evidence: security federation regressions.
- DONE: Verify CORS/CSP/security headers. Evidence: security tracker and build smoke found/fixed CSP issue.
- DONE: Verify file upload type/size/path handling. Evidence: upload/sticker tests, security tracker, release-product upload/download smoke, encoded traversal rejection in `python scripts/release_security_smoke.py --port 18109 --fuzz-iterations 120`, and `cargo test -p paracord-api --test coverage_gap_routes upload_policy_uses_active_content_downgraded_type -- --quiet`, which verifies active-content downgrades are applied before guild MIME allowlist checks so forged `Content-Type` values cannot bypass image-only policies.
- DONE: Verify attachment ownership checks. Evidence: security tracker and tests.
- DONE: Verify webhook token hashing and secret validation. Evidence: webhook code uses HMAC/hashed tokens; tests pass.
- PARTIAL: Verify invite abuse controls. Evidence: rate limits/security tracker, `cargo test -p paracord-db invites -- --quiet` covers exhausted-invite filtering, `cargo test -p paracord-api --test coverage_gap_routes invite_create_rejects_out_of_range_limits -- --quiet` verifies invite creation rejects negative/out-of-range `max_uses` and `max_age` while preserving `0` as unlimited/never, `cargo test -p paracord-api --test coverage_gap_routes public_guild_invites_are_visible_to_discovery_joiners -- --quiet` verifies invite-list visibility remains private for private guilds but works for public discovery joins, and `python scripts/release_invite_abuse_smoke.py --port 18128` verifies the same bad-bound rejections and zero sentinel behavior against a release binary before confirming a max-use-one invite is previewable before use, hidden after exhaustion, rejected for a second user, rejected for invalid codes, and omitted from guild invite listings. Gap: live abuse/raid simulation remains.
- DONE: Verify bot token handling and redaction. Evidence: security tracker and bot route tests.
- DONE: Verify audit logs for sensitive admin/mod actions. Evidence: security tracker and moderation tests.
- DONE: Verify federation signature replay protections. Evidence: security/federation tests.
- DONE: Verify private-network blocking for all outbound fetchers. Evidence: `cargo test -p paracord-api opengraph -- --quiet` passes 11 OpenGraph/private-network validation tests, including documentation IP ranges, multicast, metadata aliases, `.local`, and `home.arpa`; OpenGraph response reads now stop at the 512 KiB parse cap instead of buffering the full body first; `cargo test -p paracord-federation ssrf -- --quiet` passes 27 federation SSRF/private-address/redirect validation tests after moving federation RPCs, federated discovery, and moderation-list sync to an SSRF-checked HTTP client with automatic redirects disabled, and federated file downloads still follow redirects manually so every redirect hop gets DNS/private-network validation before the next request; federated discovery peer JSON now streams through a 512 KiB response cap, with `cargo test -p paracord-api --lib routes::discovery -- --quiet` covering route helper regressions; `docs/outbound-fetcher-inventory.md` inventories user/peer-controlled fetchers versus fixed/operator-configured integrations, including explicit short timeouts for Tenor and startup public-IP detection fixed-vendor calls, with Tenor and public-IP detection automatic redirects disabled as defense-in-depth, Tenor streamed responses capped at 1 MiB, and public-IP responses capped at 128 bytes plus IP syntax validation; `cargo test -p paracord-api routes::tenor::tests -- --quiet` and `cargo test -p paracord-server parses_detected_public_ip_only_when_valid -- --quiet` cover those fixed-vendor guards; LiveKit admin/proxy HTTP forwarding has explicit timeouts, redirects disabled, bounded admin response bodies, bounded proxy request/response bodies with the upstream response cap enforced while streaming, and `cargo test -p paracord-api livekit_proxy::tests -- --quiet` plus `cargo test -p paracord-media livekit_api_client_builds_with_release_hardening -- --quiet` cover hardened client construction plus proxy log redaction; AI provider calls validate HTTP(S) credential-free base URLs, disable automatic redirects, and cap streamed JSON responses, with `cargo test -p paracord-api ai::tests -- --quiet` covering the new guards; Tenor upstream errors no longer include reqwest URL details or upstream bodies in `ApiError::Internal` log text, avoiding accidental `key=` leakage; and `python scripts/release_security_smoke.py --port 18109 --fuzz-iterations 120` runs DAST checks for security headers, admin auth, CORS, LiveKit deny path, encoded traversal rejection, auth challenge shape, and 120 auth API fuzz iterations with no 5xx responses.

## 12. Performance/Operational Checks

- DONE: Smoke-test server startup time. Evidence: latest `python scripts/release_log_leak_smoke.py --port 18111` reached `/health` in 5.03 seconds after rebuilding the release server with current embedded UI.
- PARTIAL: Check memory under idle, chat load, voice load, screen share. Evidence: `python scripts\release_load_smoke.py --port 18110 --messages 5000 --max-page-seconds 2.0 --voice-participants 4` passed against the rebuilt release binary after extending the smoke to enable native media, create a voice channel, admit four users through an invite, join them to native voice, start/stop a native stream, leave each session, and capture RSS at idle/chat/voice phases; latest run reported idle RSS 27,832,320 bytes, post-chat-load RSS 32,821,248 bytes, and post-voice RSS 33,398,784 bytes. The smoke harness now reuses HTTP connections without carrying cookies across synthetic users, preventing Windows socket exhaustion on larger runs. Gap: real desktop capture, decoded media playback, and screen-share memory remain untested.
- PARTIAL: Check log noise in normal operation. Evidence: latest `python scripts\release_log_leak_smoke.py --port 18111` captured 4,358 bytes for startup/auth/guild/channel/webhook/Tenor failure flow. Gap: longer run/load/media log review not done.
- PARTIAL: Confirm structured logs do not leak secrets/tokens. Evidence: latest `python scripts\release_log_leak_smoke.py --port 18111` verified the captured server log did not contain the sample JWT secret, password, bearer token, webhook token, GitHub webhook secret, or a fake `PARACORD_TENOR_API_KEY`; the smoke now forces Tenor through an unreachable local HTTPS proxy so this checks the upstream-failure path without relying on external network availability. Client desktop diagnostics now recursively redact sensitive keys, bearer/JWT-like tokens, and token-bearing query parameters, with `cd client && npm run test:unit -- desktopDiagnostics.test.ts --reporter=dot` covering the redactor; API timing/error request labels now redact webhook token path segments, interaction token path segments, and sensitive query parameters, with `cd client && npm run test:unit -- client.test.ts --reporter=dot` covering the URL redactor; LiveKit proxy token-validation failures no longer log token length or secret length, and `cargo test -p paracord-api livekit_proxy::tests -- --quiet` verifies LiveKit proxy request/target/error log sanitizers redact or drop query secrets while keeping HTTP proxy forwarding bounded and redirect-free; and mocked Linux `backup-db.sh`/`restore-db.sh` smokes verify `PARACORD_DATABASE_URL=postgres://alice:secret-password@db.example.com:5432/paracord` is printed as `postgres://alice:***@db.example.com:5432/paracord`. Gap: broader manual log review under full flows remains.
- DONE: Confirm background tasks shut down cleanly. Evidence: server background workers listen on the shared shutdown `Notify`; Windows Ctrl-Break now enters the same graceful shutdown path as Ctrl-C/API shutdown, and latest `python scripts/release_graceful_shutdown_smoke.py --port 18126` starts the release binary, reaches health in 2.53 seconds, sends the interrupt, verifies the shutdown log line, and observes return code 0 without forced kill.
- DONE: Confirm backup/restore scripts still work. Evidence: `cargo test -p paracord-core sqlite_backup_restore_round_trip_includes_media -- --nocapture` passes for SQLite DB plus media; local Docker PostgreSQL 16 `pg_dump`/`pg_restore` validation restored a populated Paracord database and re-ran the PostgreSQL migration/API route smoke successfully; and the actual `scripts/backup-db.sh` plus `scripts/restore-db.sh` were copied into a disposable Linux `postgres:16` container, backed up a seeded table to `/tmp/backups/paracord-20260517-190046.dump`, restored after dropping the table, and recovered `before-backup`. The first script run found and fixed CRLF shell shebang failures by pinning `scripts/*.sh` to LF and normalizing the scripts. Follow-up mocked smokes verify both scripts redact database passwords in printed `PARACORD_DATABASE_URL` values. Admin backup download/delete/restore filename validation now rejects path traversal plus header-unsafe characters, with API route regression coverage.
- DONE: Keep operator setup scripts aligned with the current repo layout. Evidence: `scripts/setup.sh` no longer references removed `docker/docker-compose.yml`, no longer applies SQLite migrations through `psql`, builds client assets before checking the Rust workspace, points Docker users at root `docker compose up -d --build`, and `docker run --rm -v "${PWD}:/work" -w /work bash:5 bash -n scripts/setup.sh scripts/backup-db.sh scripts/restore-db.sh` passes.
- DONE: Confirm database indexes exist for new hot paths. Evidence: performance migrations exist; `python scripts/release_sqlite_query_plan_smoke.py` applies all SQLite migrations to a fresh DB and verifies SQLite uses 14 hot-path indexes for message pagination, attachment hydration, scheduled workers, login lookup, bot reviews/metrics, group E2EE sender-key lookup, slowmode lookup, pending attachment cleanup, bot guild installs, and forum thread listing; and latest Docker PostgreSQL 16 validation passes `cargo test -p paracord-db postgres_query_plan_smoke_when_configured -- --nocapture`, which runs `EXPLAIN` against 16 PostgreSQL hot paths and verifies the intended indexes are usable, including corrected bot-review ordering by `updated_at DESC, id DESC`, scheduled-event status/start lookup, message search GIN, member nick prefix search, and the new hot-path performance indexes.
- PARTIAL: Confirm large message history pagination remains responsive. Evidence: latest `python scripts\release_load_smoke.py --port 18110 --messages 5000 --max-page-seconds 2.0 --voice-participants 4` paged the latest 100 messages in 0.025s and an empty before-first page in 0.001s while also running the native voice load phase. Gap: larger production-scale histories still need staging/load validation.
- DONE: Confirm client bundle size acceptable or code-split obvious large chunks. Evidence: route-level lazy loading plus targeted Rollup vendor chunks split the previous `assets/index-BYS2i4NI.js` 2,087.50 kB entry bundle into smaller chunks; latest `cd client && npm run build` passes without Vite chunk-size warnings, with `assets/index-BXkAOfWY.js` at 341.64 kB and largest chunk `assets/vendor-livekit-DQ-t7ERF.js` at 437.47 kB. Follow-up verification: `cd client && npm run test:unit -- --reporter=dot` passes 91 files / 513 tests, and latest `cd client && npm run test:e2e` passes.

## 13. Final Release Gate

- OWNER: Clean working tree except intentional committed changes. Evidence: `git status --short --untracked-files=all` still reports hundreds of changed/untracked paths, with classification and commit plan documented. Gap: release owner must approve public/internal/discard decisions before cleanup can safely happen; fresh GitHub CI depends on intentionally staging release-critical untracked files such as root `Cargo.lock`, public release-gate docs, release smoke scripts, new migrations, and any approved `third_party/scap` vendoring strategy.
- OWNER: All intended changes committed. Evidence: `docs/release-commit-plan.md` defines the commit order. Gap: no commits should be made until owner approves scope, SDK license, vendored `third_party/scap`, and internal-report handling. CI will not represent this release candidate until those approved files are committed.
- BLOCKED: CI passes on GitHub, not just locally. Gap: not run here.
- BLOCKED: Fresh clone builds. Gap: current release candidate changes are not committed, so a fresh clone cannot represent this exact tree yet.
- BLOCKED: Fresh clone tests pass. Gap: current release candidate changes are not committed, so a fresh clone cannot represent this exact tree yet.
- BLOCKED: Fresh install smoke test passes. Gap: clean machine/VM unavailable here.
- PARTIAL: Upgrade-from-current-release smoke test passes. Evidence: latest synthetic SQLite tag-schema upgrade from `v0.9.0` passes via `python scripts\release_sqlite_upgrade_from_tag_smoke.py v0.9.0`, and latest synthetic PostgreSQL tag-schema upgrade passes via `python scripts\release_postgres_upgrade_from_tag_smoke.py v0.9.0` against a disposable PostgreSQL 16 container. Gap: no released v0.9.0 user database snapshot fixture is available locally for either database engine.
- PARTIAL: Security blockers closed. Evidence: security gate passes, P0 tracker complete, and local three-node federation validation passes. Gap: RustSec waiver and real media validation remain.
- PARTIAL: Dependency audit clean or documented with justified waivers. Evidence: latest client and SDK `npm audit --audit-level=moderate` runs report 0 vulnerabilities; `cargo audit` reports no unignored vulnerability failures; and `docs/release-risk-waivers.md` documents the current RustSec ignore/warning rationale. Gap: final release owner approval of the waivers remains.
- PARTIAL: Release notes complete. Evidence: release notes updated, including release-hardening fixes for exhausted invites, non-full-mesh federation relay, SQLite poll/emoji bool decoding, current README screenshots, create-server modal flows, composer/shared upload accessibility, scheduled-message composer behavior, invite-link flow coverage, moderation-template apply actions, account recovery/unlock flows, bot developer metrics/create-form accessibility, and client accessibility hardening. Gap: final owner review after scope/commits.
- BLOCKED: Tag only after above is true.

## Bottom Line

Do not publish a GitHub release yet. Local build/test/security/package evidence is strong, but the remaining blockers are release-critical: GitHub CI, PostgreSQL upgrade validation against a real released database snapshot, Linux packages, clean-machine install/update/uninstall, real multi-client media, deployed federation validation, and final owner decisions on working-tree scope/licensing.

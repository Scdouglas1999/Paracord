/**
 * The server home page (docs/server-home-spec.md): the first thing anybody
 * sees inside a server. `ServerHome` is the page; everything else here is one
 * of its parts, kept separate so each can be rendered and tested alone.
 */
export { ServerHome, type ServerHomeProps } from './ServerHome';

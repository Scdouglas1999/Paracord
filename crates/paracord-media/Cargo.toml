[package]
name = "paracord-media"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true

[dependencies]
paracord-models = { workspace = true }
paracord-util = { workspace = true }

# Async
tokio = { workspace = true }
dashmap = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# HTTP client for LiveKit API
reqwest = { workspace = true }
futures-util = { workspace = true }

# JWT for LiveKit tokens
jsonwebtoken = { workspace = true }

# Time
chrono = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Logging
tracing = { workspace = true }

# File handling
mime_guess = "2"
tokio-util = { version = "0.7", features = ["io"] }
uuid = { workspace = true }
urlencoding = "2"

# Crypto
rand = { workspace = true }

# S3-compatible object storage (optional)
aws-sdk-s3 = { version = "1.123.0", default-features = false, features = ["default-https-client", "http-1x", "rt-tokio", "sigv4a"], optional = true }
aws-config = { version = "1", default-features = false, features = ["credentials-process", "default-https-client", "rt-tokio", "sso"], optional = true }
aws-credential-types = { version = "1", optional = true }

[features]
default = []
s3 = ["aws-sdk-s3", "aws-config", "aws-credential-types"]
